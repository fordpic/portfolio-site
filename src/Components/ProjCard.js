import React from 'react';
import Projects from './Projects';

// Map out projects onto the below => not currrently being used
const ProjCard = () => {
	return (
		<div className='project-card'>
			<Projects />
		</div>
	);
};

export default ProjCard;
